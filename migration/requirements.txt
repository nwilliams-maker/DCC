# Additional dependencies for the database backend.
# Install alongside the app's existing requirements.txt:
#   pip install -r requirements.txt -r migration/requirements.txt
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
